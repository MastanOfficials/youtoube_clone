import React from 'react'

const ButtonList = ({name}) => {
  return (
    <div>
      <button className='p-2 m-2 bg-bg-dark btn btn-light'>{name}</button>
        </div>
  )
}

export default ButtonList